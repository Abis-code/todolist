import { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  experimental: {},
  // ✅ Removed appDir (not needed in Next.js 14+)
};

export default nextConfig;
